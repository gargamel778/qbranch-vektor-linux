# The pattern trigger makes armbian-led-state-save.sh write "hr_pattern=",
# which armbian-led-state-restore.sh rejects as a syntax error. Scrub the
# state file before the restore reads it.
[Service]
ExecStartPre=/usr/local/sbin/vektor-led-state-sanitize.sh /etc/armbian-leds.conf
