# armbian-led-state restores a saved pattern straight into sysfs. Order after
# it so its write cannot land on top of ours. The daemon also re-asserts on
# drift, so this is belt and braces.
[Unit]
After=armbian-led-state.service
